---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-server
spec:
  selector:
    matchLabels:
      app: argo-server
  template:
    metadata:
      labels:
        app: argo-server
    spec:
      containers:
      - args:
        - server
        env:
          - name: INSECURE_SKIP_VERIFY
            value: 'true'
            name: SECURE
            value: 'false'
        image: argoproj/argocli:latest
        name: argo-server
...